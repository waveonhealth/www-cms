'use strict';

/**
 * registration-account-type controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::registration-account-type.registration-account-type');
